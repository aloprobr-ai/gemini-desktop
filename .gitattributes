# Один вид переводов строки в репозитории — иначе правка из Windows
# показывается как «изменён весь файл», и сути в истории не видно.
* text=auto eol=lf

# PowerShell привычнее по-своему.
*.ps1 text eol=crlf

# Двоичное не трогать.
*.ico binary
*.png binary
*.rtf -text
